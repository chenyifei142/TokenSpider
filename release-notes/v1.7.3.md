更新类型：Bug 修复

### 修复

- 固定并收紧“年度活动 / 今日分时”切换控件尺寸，避免分时头部内容较多时控件被压缩并发生位置偏移。
- 固定活动标题宽度及头部弹性占位，避免切换到分时模式时按钮整体向右偏移；选中态统一为白色文字和 `#2076FA` 背景。
- 让年度活动页铺满活动视图容器，消除底部露出的整块空白背景。

### 验证

- 已验证 820 像素标准宽度下切换前后控件位置和尺寸保持一致。
- 已验证年度活动页完整铺满活动视图，并通过项目正式测试集。
