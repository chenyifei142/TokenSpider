更新类型：功能新增

### 新增

- DeepSeek 新增可配置的峰谷计价提示；面板显示当前价格倍数与下次切换时间，悬浮球在峰时增强橙色光晕但不显示倍率文字。
- 应用运行过程中进入 DeepSeek 高峰时段时发送一次系统通知，退出高峰、启动或切换数据源时不打扰。

### 兼容

- 峰谷提示默认关闭且仅用于展示，不修改 DeepSeek 返回的账单金额；MiMo 界面与计费行为保持不变。

### 验证

- 覆盖北京时间峰谷边界、自定义时段、面板状态、悬浮球峰时光晕以及峰时通知去重。
- 项目正式测试目录共 190 项测试全部通过。
